// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';
// this is basically a setup file for jest
// we can configure jest here
// for example we can configure jest to use a specific version of enzyme
// or we can configure jest to use a specific version of enzyme-adapter-react
// or we can configure jest to use a specific version of enzyme-to-json